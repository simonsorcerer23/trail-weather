version: '3.8'

services:
  trail-weather:
    build: .
    container_name: trail-weather
    restart: always
    ports:
      - "8502:8502"
    volumes:
      # Persist the API response cache
      - trail-weather-cache:/app/.cache
      # Persist uploaded custom trails (SQLite DB)
      - trail-weather-uploads:/app/uploads

volumes:
  trail-weather-cache:
  trail-weather-uploads:
